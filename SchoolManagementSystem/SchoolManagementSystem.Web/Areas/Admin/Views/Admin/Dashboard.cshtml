@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="text-center">Admin Dashboard</h1>
            <p class="text-muted text-center">Welcome to the Scholario admin panel!</p>
        </div>
    </div>

    <div class="row">
        <!-- Manage Students -->
        <div class="col-md-3 mb-4">
            <div class="card shadow-sm text-center">
                <div class="card-body">
                    <h5 class="card-title">Manage Students</h5>
                    <p class="card-text">View, add, update, or remove student records.</p>
                    <a href="/Admin/ManageStudents" class="btn btn-primary btn-block">Manage Students</a>
                </div>
            </div>
        </div>

        <!-- Manage Teachers -->
        <div class="col-md-3 mb-4">
            <div class="card shadow-sm text-center">
                <div class="card-body">
                    <h5 class="card-title">Manage Teachers</h5>
                    <p class="card-text">Manage teacher profiles.</p>
                    <a href="/Admin/ManageTeachers" class="btn btn-secondary btn-block">Manage Teachers</a>
                </div>
            </div>
        </div>

        <!-- Manage Schools -->
        <div class="col-md-3 mb-4">
            <div class="card shadow-sm text-center">
                <div class="card-body">
                    <h5 class="card-title">Manage Schools</h5>
                    <p class="card-text">Add, edit, or remove school data.</p>
                    <a href="/Admin/ManageSchools" class="btn btn-success btn-block">Manage Schools</a>
                </div>
            </div>
        </div>

        <!-- Manage Classes -->
        <div class="col-md-3 mb-4">
            <div class="card shadow-sm text-center">
                <div class="card-body">
                    <h5 class="card-title">Manage Classes</h5>
                    <p class="card-text">Organize classes and schedules.</p>
                    <a href="/Admin/ManageClasses" class="btn btn-warning btn-block">Manage Classes</a>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-12 text-center">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="btn btn-danger">Logout</button>
            </form>
        </div>
    </div>
</div>

<style>
    .card {
        border-radius: 10px;
    }
    .btn-block {
        width: 100%;
    }
</style>
